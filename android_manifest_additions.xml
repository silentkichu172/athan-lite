<!--
  ADD THESE INSIDE android/app/src/main/AndroidManifest.xml

  1) Add the <uses-permission> lines as direct children of the
     top-level <manifest> tag (above <application>).

  2) Add the <receiver> entries as children of <application>,
     alongside the existing <activity> tag (needed by
     flutter_local_notifications to reschedule alarms after reboot
     and to handle exact-alarm scheduling).
-->

<!-- ===== 1) Permissions (put inside <manifest ...> ) ===== -->
<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
<uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
<uses-permission android:name="android.permission.WAKE_LOCK"/>

<!-- ===== 2) Receivers (put inside <application ...>, next to <activity> ) ===== -->
<receiver android:exported="false"
    android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver" />
<receiver android:exported="false"
    android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED"/>
        <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
        <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
    </intent-filter>
</receiver>
