name: athan_lite
description: "A simple, ad-free prayer times, Qibla finder, and Islamic calendar app."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # The packages below are added by running the `flutter pub add` commands
  # listed in README.md — do NOT add them here manually, let the Flutter
  # tool pick compatible versions for your SDK.

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
